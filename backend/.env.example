# Database
DATABASE_URL="postgresql://username:password@localhost:5432/workzen_hrms"
# For SQLite fallback (uncomment and comment PostgreSQL above)
# DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="1h"

# Server
PORT=3001
NODE_ENV="development"

# SMTP Configuration (for development use MailHog or Mailtrap)
SMTP_HOST="localhost"
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@workzen.com"

# For production, use services like:
# AWS SES, Postmark, SendGrid, etc.
# SMTP_HOST="smtp.postmarkapp.com"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="your-postmark-token"
# SMTP_PASS="your-postmark-token"

# App URLs
FRONTEND_URL="http://localhost:3001"
BACKEND_URL="http://localhost:3000"